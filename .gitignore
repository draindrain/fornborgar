# Raw downloads and derived rasters — never committed (licensing + size).
# Credentials live in env vars (LANTMATERIET_USER / LANTMATERIET_PASS), never in files.
data-cache/
__pycache__/
*.pyc
.venv/
.pytest_cache/
*.egg-info/
*.aux.xml

# Built bundles: the repo keeps only `testsite` and `broborg` (national-scaleout
# §3). A batch builds here and publishes to the object host.
build/

# Claude Code local state (agent worktrees, local settings) — except the batch
# orchestration script, which is part of how this project is built.
.claude/*
!.claude/workflows/

